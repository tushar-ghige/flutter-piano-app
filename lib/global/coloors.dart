import 'package:flutter/material.dart';

class Coloors {
  static const bgColor = Colors.blueGrey;
  static const keyColor = Colors.white;
  static const accidentalKeyColor = Colors.black;
  static const borderColor = Colors.black;
  static const activeKeyColor = Color(0xFF00d8ff);
}
